import React from 'react';
import styles from './Header.module.css';

const Header = () => (
  <header className={styles.header}>
    <h1>Think | Believe | Code</h1>
  </header>
);

export default Header;
